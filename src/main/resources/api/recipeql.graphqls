type Recipe {
    id: ID!,
    name: String!,
    description: String,
    durationFrom: Int,
    durationTo: Int
}

input BasicRecipeInput {
    name: String!,
    description: String = "",
    durationFrom: Int = 0,
    durationTo: Int = 1
}